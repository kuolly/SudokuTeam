const sudoku = require('../sudoku.js'),
const solveBox = require('../test.js')


describe(boxArray, () => {
  it('find sqer and remove value', () =>{
    expect(boxArray()).toEqual()
  })
})

describe(columnArray, () => {
  it('define column', () =>{
    expect(columnArray()).toEqual()
  })
})

describe(rowArray, () => {
  it('define row', () =>{
    expect(rowArray()).toEqual()
  })
})

describe(subrunner, () => {
  it('subrunner', () =>{
    expect(subrunner()).toEqual()
  })
})
